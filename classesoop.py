import turtle

class Polygon:
    def __init__(self, name, size ):
        self.sides = sides
        self.name = name
        self.size = size
        self.interit_anges

    def draw(self):
        for i in range(self.sides)